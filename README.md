# 🗣️ TTS Patient Call

โปรแกรม **TTS Patient Call** เป็นระบบเรียกชื่อผู้ป่วยด้วยเสียงพูดอัตโนมัติ  
รองรับภาษาไทยและภาษาอังกฤษ ใช้งานร่วมกับฐานข้อมูล MySQL ได้โดยตรง  
พัฒนาเพื่อใช้ในงานห้องตรวจ หรืองานบริการที่ต้องเรียกคิวผู้ป่วย

---

## 🧩 ฟีเจอร์หลัก

✅ อ่านชื่อผู้ป่วยออกเสียงอัตโนมัติ (Text-to-Speech)  
✅ กรอกหมายเลข HN ได้ทั้งพิมพ์, ยิงบาร์โค้ด, หรือวางด้วย Ctrl+V  
✅ ตั้งค่าข้อความก่อน/หลังชื่อได้ (เช่น “เชิญคุณ”, “เข้ารับบริการที่…”)  
✅ เปิด/ปิด คำนำหน้าชื่อได้ (คุณ, นาย, นาง, เด็กชาย ฯลฯ)  
✅ จำการตั้งค่าฐานข้อมูลและข้อความไว้ใน `config.json`  
✅ รองรับ MySQL plugin `mysql_native_password`  
✅ โปรแกรมอยู่ "ข้างบนตลอดเวลา" (Always on Top)  
✅ อัปเดตอัตโนมัติจาก GitHub (ตรวจเช็กเวอร์ชันจาก `version.txt`)

---

## ⚙️ การติดตั้ง

1. ติดตั้ง Python 3.10 ขึ้นไป  
2. ติดตั้ง dependencies:
   ```bash
   pip install -r requirements.txt
